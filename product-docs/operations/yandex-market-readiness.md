# KREPI TV — готовность к монетизации Яндекс Маркета

Последняя проверка: **30 июля 2026 года**.

## Текущий внешний статус

- Рублёвая схема Реферальной программы активна: кабинет предлагает вывод
  рублей без месячного лимита, а раздел счёта для выплат доступен и заполнен.
  Платёжные реквизиты намеренно не сохраняются в репозитории.
- 30 июля пользователь подтвердил, что заявление Яндекс Маркет одобрено и
  создание реферальных ссылок стало доступно.
- Подтверждённое вознаграждение: **0 ₽**; сумма «в пути»: **0 ₽**.
- Официальный OAuth-ключ API получен и хранится только локально в `.private/`
  с правами владельца; значение не выводилось в логи и не попадает в git.
- Из редиректа штатной ссылки считан CLID партнёрских ссылок: `15238076`.
  Он может использоваться API вместо `place_id`; вручную добавлять его в
  исходные URL нельзя. Терминальное соединение с affiliate API по-прежнему
  получает VPN-блок, хотя авторизованный интерфейс Маркета работает в Chrome.
- Для точной карточки ONKRON TM5-BW (`Market ID 4559285774`) интерфейс 30 июля
  показал цену 2 606 ₽, остаток 1 и расчётную награду 612 ₽. Штатная короткая
  ссылка `https://market.yandex.ru/cc/ASvKzF` сохранена кабинетом как ссылка
  «для нерекламных размещений». Это динамические данные, не выручка и не
  разрешение обходить API-проверку перед публикацией.
- Площадка `krepitv.ru` ещё не добавлена в разделе «Блоги». Для типа «Другое»
  кабинет выдал код владения `YMReferral`; он добавлен в мета-описание главной,
  но отправка на проверку отложена до выпуска корректного TLS-сертификата.
- Сертификат `krepitv.ru` всё ещё ожидается. Это отдельный критичный блокер
  доверия и индексации, даже если заявление Маркета уже одобрено.

## Что подтверждает официальная документация

- Контентные сайты разрешены наряду с блогами, каналами и группами:
  https://yandex.ru/support/market-distr/ru/
- Самозанятый может получать рублёвое вознаграждение; программа формирует чеки,
  а налог на профессиональный доход составляет 6%:
  https://yandex.ru/support/market-distr/ru/self-employed
- Заявление на договор проверяется до двух дней, площадка — до 24 часов; после
  одобрения статус появляется в разделе «Блоги»:
  https://yandex.ru/support/market-distr/ru/partner
- API создаёт ссылку только для конкретной площадки по `place_id` или CLID и
  возвращает цену, остаток, фото и вознаграждение:
  https://yandex.ru/dev/market/affiliate/ru/reference/get-partner-link-create
- Рекламный блок обязан иметь пометку «Реклама», данные ООО «Яндекс Маркет» и
  ERID; показы по креативам нужно передавать в ЕРИР. Официальный FAQ отдельно
  указывает, что размещение на товарной онлайн-витрине рекламой не считается;
  режим размещения нужно фиксировать явно, а не угадывать по виду ссылки:
  https://yandex.ru/support/market-distr/ru/ad-labeling/
- Заказ подтверждается только после получения и 14 дней без заявки на возврат;
  минимальная сумма к выплате — 300 ₽:
  https://yandex.ru/support/market-distr/ru/terms

## Безопасная последовательность запуска

1. Дождаться корректного TLS, затем отправить `krepitv.ru` с кодом
   `YMReferral` на проверку в разделе «Блоги» и сохранить выданный `place_id`.
2. Убрать VPN-блок для affiliate API. OAuth уже хранится только в `.private/`,
   а CLID `15238076` можно передать API вместо `place_id`.
3. Для рекламных креативов получить ERID. Для товарной онлайн-витрины хранить
   явное подтверждение нерекламного режима и не смешивать его с рекламными CTA.
   В обоих случаях формировать ссылки API для точных карточек и проверять их.
4. Публиковать CTA лишь когда одновременно проходят identity, stock, reward,
   freshness, place и marking gates. При любом сбое карточка остаётся полезной,
   а рекламная кнопка скрывается.
5. Дождаться сертификата с `krepitv.ru` в SAN, включить Enforce HTTPS и проверить
   canonical, robots и sitemap без `--insecure`.
6. Считать доходом только подтверждённое вознаграждение после отмен и возвратов;
   клики, заказы «в пути» и рекламные обещания выручкой не считать.

## Измеримый путь к 100 000 ₽

Фиксировать еженедельно:

1. уникальные органические посетители;
2. переходы на Маркет по `VID`, привязанному к странице и товару;
3. оформленные, доставленные, отменённые и возвращённые заказы;
4. подтверждённое вознаграждение и фактическое среднее на заказ;
5. доход на 1 000 органических посетителей.

План продаж нельзя строить на витринной сумме награды. После первых 30
подтверждённых заказов прогноз считается по фактическому среднему после возвратов:

```text
нужные подтверждённые заказы = 100 000 ₽ / среднее вознаграждение за заказ
нужный органический трафик = нужные заказы / фактическая конверсия посетитель → подтверждённый заказ
```

До появления этих данных продуктовый KPI — не число сгенерированных страниц, а
первые индексируемые входы по точным моделям и первый подтверждённый заказ.
