# Сведение рабочих изменений — 22 сентября 2026

## Объём

Текущий этап по последнему сообщению владельца — сведение уже сделанных изменений и чистый релиз. Отдельный запрос на новый спринт из десяти страниц ещё не реализован; этот релиз не считается его выполнением.

- Локальная `main` безопасно обновлена до удалённой: 24 ранее опубликованных коммита, без перезаписи локальных файлов.
- Единственный формально неслитый коммит старой ветки подбора предложений уже присутствовал в `main` как эквивалентный patch. Ветка слита в историю отдельным merge-коммитом без изменения проверенного дерева. Единственный конфликт был в документации: сохранена более полная актуальная запись о тестах. Неслитых локальных веток больше нет.
- Сохранены локальные результаты семи запросов Wordstat от 3 сентября вместе с исходными ответами. Это исторический замер, не свежая статистика и не прогноз посещений. Описание операторов уточнено по фактически отправленным запросам.
- Корневой lockfile сохранён; зависимости интерфейса по-прежнему фиксируются отдельным `web/package-lock.json`.
- Правила исключения зависимостей охватывают и обычные каталоги, и локальные символьные ссылки на них.
- Приватные аналитические выгрузки и credentials не включаются в git. Старые сгенерированные файлы другой рабочей ветки сохранены в именованном локальном git stash `preserve superseded generated artifact before consolidation 2026-09-22`; они не заменяют текущий production artifact. Незакоммиченного исходного кода в этой старой ветке не было.

## Снятая статистика

Срез получен напрямую из API 22 сентября; сырые выгрузки остаются в `.private`.

- Google, финальные данные 24 августа — 20 сентября: 3 114 показов, 59 кликов, CTR 1,89%, средняя позиция 12,97. Часовой пояс отчёта — America/Los_Angeles. Это поисковые клики, не уникальные посетители.
- Google URL Inspection: проверены 326 URL sitemap; 270 PASS, 56 NEUTRAL, технических ошибок запросов нет. Это результат проверки данного набора URL, а не отдельный полный отчёт покрытия Search Console.
- Три страницы релиза 19 сентября (раздача интернета с телефона, просмотр без интернета, универсальный пульт) пока имеют статус «URL неизвестен Google». Последний обход не указан; это не подтверждение отклонения качества.
- Яндекс, 8–21 сентября: 491 показ, 32 клика. Агрегат индексирования: 75 страниц в поиске, 17 исключённых. Список страниц в поиске содержит 88 строк и все три новых URL; агрегат и список обновляются несинхронно, поэтому 88 не подменяет значение агрегата.
- Метрика, полные дни 8–21 сентября: 61 пользователь и 62 визита из поиска после исключения тестовых заходов; 3 достижения цели перехода на Маркет. Только аудитория с работающей аналитикой, не все посетители. По этому отчёту семидневная цель 1 001 пользователя в сутки не достигнута.
- У инструментов в общей измеряемой аудитории 7 стартов и 0 завершений. Не делать вывод о причине по такой выборке без исследования поведения.

У разных систем разные окна и определения метрик: числа нельзя складывать. Сравнения одинаковых недель здесь не выполнялись, поэтому вывод о росте или снижении не заявляется.

## Проверка и выпуск

Полный `npm run build` завершился успешно, включая Rust, WASM, русскую копию, каталог, аналитику, партнёрские контракты и проверку сайта. Повторная сборка не изменила HTML, JavaScript, CSS и sitemap; локально обновился только платформенно-зависимый WASM artifact, как предусмотрено CI.

Релиз исходников `d34ada7e` опубликован; merge-коммит `534115b0` имеет то же дерево. Удалённые source CI и GitHub Pages завершились успешно. Общий реестр skills/harnesses синхронизирован; финальный `check --strict` прошёл без drift.

Production-проверка после публикации:

- HTTPS отвечает 200, проверка сертификата успешна; HTTP перенаправляет на HTTPS с 301.
- Девять контрольных файлов (главная, три новых помощника, карточки модели и кронштейна, sitemap, robots и WASM) побайтно совпали по SHA-256 с artifact именно опубликованной GitHub Pages сборки.
- Все три помощника завершили расчёт в независимых headless-профилях на 320, 768 и 1440 px. В каждом сценарии — одно событие результата и отсутствие горизонтального переполнения. Сохранён отказ от аналитики, поэтому проверки не пополняют реальную аудиторию; пользовательский Chrome не использовался.
- Sitemap сохраняет 326 URL. Новые страницы в этом этапе не создавались, повторное уведомление IndexNow без содержательного изменения не отправлялось.
- Все десять локальных рабочих копий проверены: незакоммиченных изменений нет. Старый generated artifact сохранён в stash, не удалён.
