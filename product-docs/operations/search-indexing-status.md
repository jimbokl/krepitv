# KREPI TV — статус индексации

Последняя проверка: **30 июля 2026 года**.

## Production и HTTPS

- Авторитетный DNS REG.RU и публичные резолверы направляют `krepitv.ru` и
  `www.krepitv.ru` на GitHub Pages.
- TLS исправлен: сертификат Let's Encrypt содержит оба домена, принудительный
  HTTPS включён, HTTP перенаправляет на HTTPS, а сайт по HTTPS отвечает без
  ошибки имени сертификата.
- Следующий релиз должен добавить файл проверки Яндекс Вебмастера и подключение
  Метрики с явным согласием посетителя. До публикации эти две части нельзя
  считать работающими в production.

## Google Search Console

- Domain property `krepitv.ru` подтверждён через DNS TXT.
- `https://krepitv.ru/sitemap.xml` отправлен и 30 июля 2026 года обработан со
  статусом «Успешно».
- Search Console обнаружил в sitemap **110 URL**. Это число обнаруженных
  страниц, а не подтверждение их индексации или поискового трафика.

## Яндекс Вебмастер

- HTTPS-площадка `https://krepitv.ru` добавлена в Яндекс Вебмастер.
- Подготовлен корневой HTML-файл проверки
  `yandex_ea4fee98996786d2.html`. Подтверждение в интерфейсе ещё не нажато:
  сначала файл должен попасть в production и стабильно отвечать по HTTPS.
- Sitemap в Яндекс Вебмастер пока не отправлен. Его нужно добавить после
  успешного подтверждения прав на площадку.

## Яндекс Метрика

- Для `krepitv.ru` создан отдельный счётчик **111176777**.
- Подключение реализовано с явным согласием: до выбора «Разрешить аналитику»
  счётчик не загружается. Вебвизор, карта кликов и аналитика форм отключены.
- Код цели `market_click` подготовлен на сайте, но цель с таким идентификатором
  ещё нужно создать в интерфейсе Метрики после публикации и обнаружения тега.
- До этого релиза и проверки цели текущие HTTP-запросы и тестовые визиты нельзя
  использовать как доказательство органического трафика.

## Следующая контрольная точка

1. Опубликовать текущую сборку и проверить по HTTPS файл Яндекс Вебмастера,
   sitemap, canonical и robots.
2. Подтвердить площадку в Яндекс Вебмастере и отправить
   `https://krepitv.ru/sitemap.xml`.
3. Убедиться, что Метрика не загружается без согласия, а после согласия видит
   счётчик `111176777`; затем создать и проверить цель `market_click`.
4. Отслеживать отдельно индексирование, органические переходы и исходящие клики.
   Sitemap, HTTP 200, показы и тестовые визиты посетителями не считать.
