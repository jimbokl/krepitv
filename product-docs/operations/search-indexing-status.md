# KREPI TV — статус индексации

Последняя проверка: **31 июля 2026 года**.

## Production и HTTPS

- Авторитетный DNS REG.RU и публичные резолверы направляют `krepitv.ru` и
  `www.krepitv.ru` на GitHub Pages.
- TLS исправлен: сертификат Let's Encrypt содержит оба домена, принудительный
  HTTPS включён, HTTP перенаправляет на HTTPS, а сайт по HTTPS отвечает без
  ошибки имени сертификата. Контроль 31 июля: сертификат `CN=krepitv.ru`
  содержит SAN `krepitv.ru` и `www.krepitv.ru`, действует до 28 октября
  2026 года; `curl` подтверждает цепочку с `ssl_verify_result=0`.
- Продуктовый релиз `a485a63` и исправление CLI `33c7675` опубликованы;
  GitHub Pages подтвердил `built`, а девять ключевых production-артефактов
  побайтно совпали с локальной сборкой.
  Файл проверки Яндекс Вебмастера доступен по HTTPS, Метрика подключается
  только после явного согласия посетителя, а 12 проверенных предложений Маркета
  отдаются на товарных страницах без региональных цен.
- Релиз `f05a7c6` усилил существующую карточку ONKRON TM6 без создания нового
  URL: добавлены проверяемые характеристики, 77 связей `verified-fit` и три
  FAQ. GitHub Pages подтвердил `built`; production HTML и sitemap побайтно
  совпали с локальным артефактом. Изолированный browser smoke подтвердил одну
  свежую прямую кнопку `market.yandex.ru` с CLID и безопасным `rel`, без цены и
  без удалённого партнёрского дисклеймера.
- Релиз `3496b79` добавил компактные клиентские витрины на шесть существующих
  SEO-хабов: ONKRON, KROMAX, Holder, iTECHmount, общее сравнение и выдвижные
  кронштейны. GitHub Pages подтвердил `built`; production HTML, JS, CSS,
  affiliate snapshot и sitemap побайтно совпали с локальными артефактами.
  Изолированный mobile browser smoke подтвердил точное число карточек на всех
  шести URL — всего 16 размещений — и прямые ссылки `market.yandex.ru` с
  CLID/VID, безопасным `rel`, без цены и без горизонтального overflow.
- Production sitemap до выпуска справочника винтов содержал **141 уникальный
  URL**. Страницы брендов ТВ,
  диагоналей 43/50/55/65/75″ и VESA 200×200/300×200/300×300/400×400
  индексируемы; страница 32″ остаётся `noindex` и исключена из
  sitemap из-за недостаточного наполнения каталога.
- У всех URL sitemap есть проверяемый ISO `lastmod`: для моделей и
  кронштейнов он связан с датой проверки характеристик и датой коммерческого
  профиля, для информационных и доверительных страниц — с датой изменения
  соответствующего содержания. Сборка отклоняет будущие даты, query/fragment,
  повтор pathname, несовпадение canonical и неточный `lastmod`.
- Все 80 карточек моделей имеют **208** обычных контекстных ссылок на
  существующие индексируемые брендовые, диагональные и VESA-хабы. Описания 23
  кронштейнов сокращены до 160 символов: фактический максимум — 127. На всех
  страницах подключён локальный favicon.
- Опубликованы три страницы под подтверждённый спрос: коммерческое сравнение
  всех 23 кронштейнов, выдвижные модели с расчётом вылета и точный каталог
  ONKRON. Длинные каталоги сгруппированы по брендам и скрыты в нативных
  раскрывающихся блоках. Региональные цены не копируются, а необязательный
  длинный партнёрский дисклеймер удалён. Проверка сборки запрещает его возврат
  не только в HTML, но и в клиентском JS и публичных JSON; там же запрещены
  числовые цены.
- Статический HTML не содержит действующих партнёрских URL: там остаётся
  нейтральный placeholder. Прямая кнопка Маркета появляется клиентом только
  после проверки свежего same-origin снимка; просроченные, повреждённые и
  перенаправленные на другой origin данные отклоняются.
- Непустой официальный API-check подтверждён на GitHub runner: 17 карточек,
  12 publishable-офферов, полный build и production deploy прошли. Снимок
  обновляется дважды в сутки без внешнего Chrome; OAuth хранится только в
  GitHub Secret и закрытом локальном файле.
- Вторая волна добавила KROMAX, Holder, iTECHmount, Hisense, TCL, Xiaomi,
  диагонали 50/75″ и VESA 300×300/400×400. У каждой страницы есть измеренный
  строгий спрос, собственный каталог и минимум 25 verified-fit пар. Сборка
  блокирует индексирование тонких фильтров; поэтому 85″ и VESA 600×400 пока не
  опубликованы.
- В релизе `828f5ae` без создания новых URL усилены десять существующих
  карточек точных моделей и кронштейнов с измеренным спросом и живыми
  предложениями Маркета. Один версионированный источник данных управляет
  статическим Rust-блоком и клиентским интерфейсом; сборка сверяет заявленные
  количества с текущим графом совместимости. FAQ остаются под раскрывающимися
  блоками, числовые цены не публикуются.
- Мобильный Lighthouse на товарной странице после релиза показал Performance
  99, Accessibility 100, Best Practices 100 и SEO 100; FCP и LCP — 1,1 с,
  TBT — 80 мс, CLS — 0. Основной и hover цвета затемнены, а минимальный
  контраст белого текста закреплён автоматическим WCAG AA-тестом.
- Модельная атрибуция не создаёт дополнительные посадочные: все 80 карточек
  используют существующие canonical URL.
  Отдельный свежий снимок может показать до трёх предложений
  на модель; отсутствие конкретного оффера не меняет индексируемый HTML и не
  создаёт тонкую страницу. Само наличие CTA или уникального `VID` не считается
  индексацией, показом, визитом или доходом.

## Production-релиз справочника винтов — 31 июля

- Production artifact коммита `824500f2a27d1b96e3331eab008c3e993e67ad75`
  содержит **142 canonical URL**: к прежней
  матрице добавлен ровно один доказательный URL
  `/vinty-dlya-krepleniya-televizora/`.
- Страница имеет собственный поиск по 26 точным моделям, официальные источники
  Samsung, Hisense и TCL и взаимные ссылки с модельными карточками. Длинные
  перечни скрыты по брендам, цены и партнёрская витрина отсутствуют.
- GitHub Pages завершил сборку со статусом `built`. Страница и sitemap
  побайтно совпали с локальными артефактами; HTTPS отвечает 200 с
  `ssl_verify_result=0`, JS и CSS отвечают 200 с правильным MIME.
- Release gate и production browser QA на 390 и 1440 px прошли без
  горизонтального overflow. IndexNow принял ровно новый URL с HTTP 202. Это
  уведомление, а не обнаружение, индексация, показ или органический посетитель.

## Google Search Console

- Domain property `krepitv.ru` подтверждён через DNS TXT.
- `https://krepitv.ru/sitemap.xml` отправлен и 30 июля 2026 года обработан со
  статусом «Успешно».
- На момент последней внешней проверки Search Console обнаружил **110 URL**
  предыдущего sitemap. Текущий production sitemap уже содержит **141 URL**, но
  до повторной обработки нельзя считать все 141 обнаруженными или
  проиндексированными Google.
- 31 июля общий отчёт «Страницы» и отчёт эффективности всё ещё показывали
  «Данные обрабатываются». Проверка конкретных URL дала более точный результат:
  главная и `/modeli/samsung-qe55q70dauxru/` обнаружены через sitemap, но имеют
  статус **«Обнаружена, не проиндексирована»**; сканирование ещё не выполнялось.
- Для этих двух URL отправлены одиночные запросы на индексирование, и Search
  Console подтвердил добавление в приоритетную очередь. Очередь не является
  индексацией, показом или посетителем; повторно отправлять те же URL не нужно.

## Яндекс Вебмастер

- Права на HTTPS-площадку `https://krepitv.ru` подтверждены 30 июля 2026 года
  корневым HTML-файлом `yandex_ea4fee98996786d2.html`.
- `https://krepitv.ru/sitemap.xml` добавлен в Яндекс Вебмастер и находится в
  очереди на обработку с 30 июля. Очередь не является индексацией или трафиком.
- В релизе `48e735d` добавлен fail-closed IndexNow-клиент с публичным ключом и
  отдельным реестром изменённых URL. После проверки key-файла на production
  Яндекс 31 июля принял пакет из **11** содержательно изменённых индексируемых
  страниц с HTTP 202. Страница 32″ и страницы, где изменился только хеш ассета,
  не отправлялись. Ответ 202 означает приём уведомления, а не обход, индексацию,
  показ или посетителя.
- После публикации `bbb40b9` клиент дополнительно сверил с sitemap все 80
  модельных и 22 страницы кронштейнов. Яндекс принял второй пакет из **102 URL**
  с HTTP 202. Он также не считается фактическим обходом или индексацией.
- После публикации `98f05ed` клиент сверил с новым sitemap три новых хаба,
  три содержательно обновлённых хаба механизмов и все 22 страницы кронштейнов.
  Яндекс принял пакет из **28 URL** с HTTP 202. Это только уведомление об
  изменениях, не индексирование, показ, визит или доход.
- После публикации `c5fd858` клиент выделил точным diff десять новых хабов,
  обновлённые хабы, 46 карточек телевизоров с новыми контекстными ссылками и
  17 карточек кронштейнов новых брендовых групп. Яндекс принял **82 URL** с
  HTTP 202. Это также только уведомление об изменениях.
- После публикации `75cecdd` клиент уведомил о **92 URL**, на которых
  статическая партнёрская ссылка заменена fail-closed placeholder: 80 карточек
  моделей и 12 карточек кронштейнов с доступными офферами. Яндекс принял пакет
  с HTTP 202; это не подтверждает обход или индексацию.
- 31 июля при попытке уведомить только `/o-proekte/` обнаружена ошибка CLI:
  без флага `--manifest` он отбрасывал первый позиционный URL и использовал
  текущий манифест. Из-за этого Яндексу повторно отправлены 10 уже известных
  URL; это безопасное уведомление, а не трафик или индексация. Ошибка исправлена
  в `33c7675`, закреплена тестом и сухим прогоном на один URL. После исправления
  `/o-proekte/` отдельно принят IndexNow как **1 URL** с HTTP 202.
- После релиза `f05a7c6` сухой прогон выделил только изменённую карточку
  `/kronshteyny/onkron-tm6/`. IndexNow принял этот **1 URL** с HTTP 200. Это
  уведомление об изменении, а не подтверждение обхода, индексации, показа,
  посетителя или дохода.
- После релиза `3496b79` сухой прогон выделил только шесть усиленных SEO-хабов.
  Первая попытка завершилась сетевым таймаутом до HTTP-ответа; повторный
  идемпотентный пакет Яндекс принял как **6 URL** с HTTP 202. Это уведомление,
  а не подтверждение обхода, индексации, показа, посетителя или дохода.
- Проверка 31 июля до релиза `b14e491`: **0 проиндексированных страниц**,
  исключённых страниц нет;
  в статистике запросов за доступный период до 28 июля — **0 показов и
  0 кликов**. Публикация 140 URL этот факт сама по себе не меняет.

## Яндекс Метрика

- Для `krepitv.ru` создан отдельный счётчик **111176777**.
- Подключение реализовано с явным согласием: до выбора «Разрешить аналитику»
  счётчик не загружается. Вебвизор, карта кликов и аналитика форм отключены.
- Цель «Переход на Яндекс Маркет» создана с точным идентификатором
  `market_click`. Тестовое событие принято endpoint Метрики вместе только с
  техническими идентификаторами оффера, страницы и VID.
- Прямой визит с `metrika-test=1` и тестовое достижение цели не являются
  органическим трафиком и должны исключаться из продуктовой метрики.
- Во время проверки 31 июля Метрика предупреждала о технических работах и
  скрывала статистику отчёта. Это не интерпретируется ни как нулевой, ни как
  положительный трафик; контроль продолжается после восстановления отчётов.

## Следующая контрольная точка

1. Дождаться обработки sitemap в Яндекс Вебмастере и отчётов Search Console,
   не отправляя файл повторно без ошибки.
2. Проверять, что Метрика не загружается без согласия, а цель `market_click`
   получает только обезличенные технические параметры.
3. Отслеживать отдельно индексирование, органические переходы и исходящие клики.
   Sitemap, HTTP 200, показы и тестовые визиты посетителями не считать.

## Публичный контроль и SEO-волна 6 — 31 июля

- Публичные запросы `site:`/`url:` в Яндексе и точные доменные запросы в Bing
  пока не нашли документы `krepitv.ru`. Google публично не подтверждён из-за
  антибот-защиты; проверяется только через Search Console.
- Техническая доступность исправна: robots и sitemap отвечают 200, sitemap
  содержит 140 URL, главная и три приоритетные TCL-страницы отдают SSR title,
  H1, self-canonical и не содержат `noindex`.
- Следующей существующей страницей для измеримого улучшения выбрана
  `/kronshteyny-onkron/`: строгий спрос Wordstat 8 464, пять точных моделей и два
  свежих оффера. Title/H1 и URL сохранены; добавлены сравнение, сильная ссылка из
  `/kronshteyny/` и внутренняя проверка перед переходом на Маркет.
- Публичное отсутствие результата и технический HTTP 200 не интерпретируются
  как органический трафик. Базовый подтверждённый результат остаётся 0.
- Релиз `37c6c73` опубликован успешным Pages run `30603356639`. После проверки
  production IndexNow принял пять изменённых существующих URL с HTTP 202:
  четыре брендовых хаба и `/kronshteyny/`. Ответ не считается обходом,
  индексацией, показом или посетителем.

## Коммерческий маршрут и паспорта крепления TCL — 31 июля

- Релиз `4b4691b65de36b42c854f6a11d86631272187f68` опубликован успешным
  GitHub Pages run `30604655624`. Production HTML коммерческой страницы и
  карточки TCL 65C7K побайтно совпал с локальной сборкой; все шесть изменённых
  продуктовых URL отвечают HTTP 200.
- На `/kupit-kronshteyn-dlya-televizora/` перед полным каталогом добавлено
  компактное сравнение трёх сценариев выбора. На production доступны ровно три
  свежие прямые кнопки `market.yandex.ru` с CLID, VID, идентификатором и рангом
  размещения, безопасным `rel`, без числовой цены и без удалённого длинного
  партнёрского дисклеймера.
- Пять существующих карточек TCL получили проверяемые паспорта настенного
  крепления: VESA, группы винтов, количество, необходимость адаптеров,
  региональность источника и дату проверки. Данные добавлены только для
  55C6K, 55C7K, 55C7L, 65C7K и 75C6K; на остальные модели они не размножены.
- Полный контроль релиза прошёл **200 тестов**. Browser QA при ширине 1440 px
  для коммерческой страницы и 390 px для TCL 65C7K не обнаружил
  горизонтального переполнения.
- После проверки production IndexNow принял пакет из **6 URL** с HTTP 202:
  коммерческую страницу и пять карточек TCL. Это только уведомление об
  изменениях, не подтверждение обхода, индексации, показа или посетителя.
- Финансовый baseline перед релизом: **0 заказов**, **0 ожидаемого** и
  **0 подтверждённого вознаграждения**. Текущий результат цели — 0 ₽ из
  100 000 ₽ подтверждённого ежемесячного вознаграждения.

## SEO-волна 8 — паспорта крепления Hisense, 31 июля

- Без создания новых URL усилены шесть существующих карточек: Hisense 55U7S,
  65U7S, 55U7S PRO, 55E77SL, 55U7Q и 43E7S. Sitemap остаётся на **140 URL**.
- Для каждой модели добавлен паспортный размер VESA, диаметр и количество
  винтов, а диапазон L из схемы отделён от полной длины винта. Полная длина
  зависит от планки кронштейна, шайбы и предусмотренной вставки, поэтому сайт
  её не угадывает.
- У 55U7S PRO обнаружено расхождение официальных источников: карточка модели
  указывает VESA 400×300, связанное российское руководство — 400×400. Карточка
  показывает оба значения и работает в fail-closed режиме до ручной проверки.
- Полная сборка прошла **202 теста**. Browser QA на ширине 390 и 1440 px не
  обнаружил горизонтального переполнения; утверждённый дизайн сохранён.
- Перед публикацией синхронизация партнёрского отчёта показала **0 заказов** и
  **0 ₽ подтверждённого вознаграждения**. Наличие страницы или оффера не
  считается доходом.
- Релиз `0e8553c` опубликован успешным GitHub Pages run `30605884954`.
  Все шесть URL отвечают HTTP 200; production HTML карточек 55U7S и 55U7S PRO
  побайтно совпал с локальной сборкой. Удалённый длинный партнёрский дисклеймер
  на проверенных страницах отсутствует.
- После production-проверки IndexNow принял ровно эти **6 URL** с HTTP 200.
  Ответ означает только приём уведомления об изменениях и не считается
  обходом, индексацией, показом, посетителем или доходом.

## SEO-волна 9 — Samsung U8000F и Hisense U77SL/E7S, 31 июля

- Без создания новых URL усилены семь существующих карточек: Hisense 50U77SL,
  65U77SL, 50E7S, 55E7S и Samsung UE43U8000FUXRU, UE50U8000FUXRU,
  UE55U8000FUXRU. Их суммарный точный модельный спрос — **27 978**, на
  момент проверки доступно **19** живых размещений Маркета.
- Для Samsung добавлены VESA 200×200, четыре M8 и паспортный размер C
  23–25 мм после монтажной пластины. C не выдаётся за полную длину винта;
  неизвестное наличие проставок отображается явно.
- Для Hisense добавлены паспортные M6 и диапазоны L. На 50E7S/55E7S отдельно
  показаны две обязательные верхние монтажные детали. Для 65U77SL опубликована
  оговорка: официальный support связывает модель с общим PDF, однако код
  65U77SL на его страницах отсутствует, а имя файла относит 65/75/85 к U7S.
- Sitemap остаётся на **140 URL**. Полная сборка прошла **202 теста**;
  числовые цены и удалённый длинный партнёрский дисклеймер отсутствуют.
- Синхронизация отчёта перед релизом: **0 заказов** и **0 ₽ подтверждённого
  вознаграждения**. Спрос, доступный оффер и публикация страницы деньгами не
  считаются.
- Релиз `955206dbd84df0d6bd4cbe9c7bfff6c4df9bb9b1` опубликован успешным
  GitHub Pages run `30607259109`. Все семь URL отвечают HTTPS 200 и побайтно
  совпадают с локальной сборкой; Samsung показывает C 23–25 мм и неизвестный
  статус проставок, а исправленная оговорка 65U77SL присутствует.
- После проверки production IndexNow принял ровно эти **7 URL** с HTTP 202.
  Это уведомление об изменениях, не подтверждение обхода, индексации, показа,
  посетителя, заказа или дохода.

## SEO-волна 10 — Samsung Q7F/F6000F, Hisense U8Q/U7Q/UR9S и TCL P6K/P7K/S5K, 31 июля

- Без создания новых URL усилены девять существующих карточек. Их суммарный
  точный модельный спрос Wordstat — **22 213**, на момент релиза доступно
  **27** живых размещений Маркета. Sitemap остаётся на **140 URL**.
- Для Samsung QE43Q7FAAUXRU и QE50Q7FAAUXRU добавлены четыре M8 и диапазон C
  19–21 мм, для UE32F6000FUXRU — четыре M8 и C 21–23 мм. Комплектные M4×L14
  последней модели явно отделены как винты ножек, а не VESA.
- Для Hisense 65U8Q, 65U7Q и 65UR9S добавлены четыре M6, диапазон L
  9,5–11,5 мм и обязательное предупреждение о промежуточных вставках. Прямой
  PDF и страница поддержки точной модели хранятся раздельными источниками.
- TCL 55P6K работает fail-closed: два официальных документа расходятся по
  верхнему пределу 26/28 мм, поэтому тип M6 подтверждён, а единая длина не
  публикуется. TCL 55P7K показывает две M6×16 и две M6×30 без выдуманной
  привязки к рядам. Для TCL 43S5K паспорт винтов намеренно отсутствует, потому
  что российская поддержка не публикует точные данные.
- Разрешённый набор из **26** паспортов закреплён точными ID в Rust- и
  JavaScript-валидаторах. Полная сборка прошла **202 теста**. Визуальная
  проверка TCL 55P6K и 55P7K при ширине 390 и 1440 px не обнаружила
  горизонтального переполнения; утверждённый дизайн сохранён.
- Релиз `61e6596fab6489991549c3c853243fd127528910` опубликован успешным GitHub
  Pages run `30609466036`. Все девять URL отвечают HTTPS 200, проверка TLS
  возвращает 0, а production HTML побайтно совпадает с локальной сборкой. На
  восьми страницах присутствует ровно один паспорт, у 43S5K — ни одного.
- После production-проверки IndexNow принял ровно эти **9 URL** с HTTP 202.
  Это только уведомление об изменениях, не обход, индексация, показ, посетитель,
  заказ или доход.
- Синхронизация партнёрского отчёта в 06:21 UTC показала **0 заказов**, очередь
  решения пуста, подтверждённое вознаграждение — **0 ₽**. Текущий результат
  цели остаётся 0 ₽ из 100 000 ₽ в месяц.
- По результатам portable growth playbook дальнейшее размножение URL
  заморожено до зрелой query→page выборки. Следующий продуктовый приоритет —
  consent-gated событие фактического результата подбора и единая воронка от
  органического landing до `APPROVED`-вознаграждения; просмотры страниц
  результатом не считаются.

## Измерительный релиз после portable growth playbook — 31 июля

- Релиз `8aa2314bf36db8301d54adfef1eb5e69a9ad905b` опубликован GitHub Pages со
  статусом `built`. Главная, `/podbor/`, политика конфиденциальности, `/vesa/`,
  калькулятор высоты и новый JS-артефакт побайтно совпали с локальной сборкой;
  HTTP перенаправляет на HTTPS, проверка TLS возвращает 0.
- Добавлена единая consent-gated цель `result_completed`: семь калькуляторов
  фиксируют её только после успешного явного запуска, мастер подбора — после
  явного выбора механизма и непустого результата. Загрузка страницы и React-
  рендер событием не считаются.
- В событие не входят модель телевизора, механизм, параметры стены, размеры и
  текст поиска. Разрешены только контролируемые тип инструмента и результата,
  безопасный путь страницы и ограниченное количество вариантов. До согласия
  код Метрики не загружается.
- Блок согласия проверен на прямых входах в главную и `/podbor/`; QA при ширине
  390 px не обнаружил горизонтального переполнения или перекрытия интерфейса.
  Полная сборка и дополнительные regression-тесты прошли; суммарно проверено
  **215 тестов**.
- В интерфейсе Метрики наличие цели `result_completed` пока не подтверждено:
  сохранённый идентификатор разрешённого окна Chrome после внешнего
  перезапуска больше не существует, а другие окна по правилу безопасности не
  открывались и не читались. До проверки настройки успешные вызовы не следует
  считать наблюдаемыми достижениями цели.
- Контрольная синхронизация заказов до 06:41 UTC: **0 заказов**, **0 ожидающих
  решения**, **0 ₽ подтверждённого вознаграждения**. Измерительный релиз сам по
  себе не меняет финансовый baseline 0 ₽ из 100 000 ₽ в месяц.

## Релиз перехода «результат → карточка кронштейна» — 31 июля

- Коммит `426ff26211e68d2a31bc3ccde1af7df5ef179499` опубликован успешным GitHub
  Pages run `30611606507`. HTTP перенаправляет `/podbor/` на HTTPS, production
  отвечает 200, TLS verify равен 0.
- Production JS `main-B1J97rl2.js` и страница политики конфиденциальности
  побайтно совпали с локальным релизом. В JS присутствует новая цель
  `mount_detail_click`; числовые цены и удалённый длинный дисклеймер не
  вернулись.
- Production browser E2E прошёл путь TCL 55C7K → бетон → фиксированный механизм
  → три первых результата → карточка iTECHmount P4F. Длинный список из 14
  вариантов остался закрыт, внутреннее событие содержало только
  `{placement: "featured_result"}`, конечный pathname совпал с href.
- Sitemap остался на **140 URL**. Это продуктовый и измерительный релиз без
  новых индексируемых страниц, поэтому массовое уведомление IndexNow не
  отправлялось.
- Полная сборка прошла **223 теста**. Visual QA на 390, 900 и 1440 px не выявил
  горизонтального переполнения; на планшете короткий список имеет две колонки.
- Контрольная синхронизация заказов до `2026-07-31T07:06:34.361Z`: **0
  заказов**, **0 ожидающих решения**, **0 ₽ подтверждённого вознаграждения**.
  Публикация релиза не меняет baseline 0 ₽ из 100 000 ₽ в месяц.

## Безопасное ранжирование в подборе — 31 июля

- Коммит `32745f57774f279328e1b2415a81ca62c9cf929d` опубликован успешным GitHub
  Pages run `30612538031`. Production использует `main-Dvte_cMm.js`; его
  SHA-256 побайтно совпал с локальным артефактом:
  `ba711c9ac55e7210e42a221b618b02d66c21589e0b9577d28dc663222f430e16`.
- `/podbor/` теперь ранжирует только по правилу `fit_status → score DESC →
  свежий точный оффер → исходный порядок`. Аудит 80 моделей × 3 механизма
  подтвердил 577 из 719 офферных мест top-3 против 365 baseline, без снижения
  score и без потери точного механизма.
- Production browser QA для `TCL 55C7K / fixed` показал: `iTECHmount P4F`
  остаётся первым как точный fixed с score 118; `KROMAX ATLANTIS-45` и
  `KROMAX DIX-18` следуют с равным score 110 и живыми карточками Маркета.
  Событие внутреннего перехода содержит только
  `{placement: "featured_result"}`. На ширине 390 px нет переполнения, числовых
  цен и удалённого длинного дисклеймера; хвост из 14 вариантов закрыт.
- Переход через собственную карточку `KROMAX ATLANTIS-45` ведёт напрямую на
  `market.yandex.ru` с точным VID `krepitvKromaxAtlantis45`, CLID `15238076` и
  `rel="sponsored nofollow noopener noreferrer"`.
- Официальная синхронизация заказов до `2026-07-31T07:23:01.612Z`: **0
  заказов**, **0 в карантине**, **0 ожидают решения**, **0 ₽ подтверждённого
  вознаграждения**. Релиз и рост покрытия офферами не считаются выручкой.
- Management API Метрики 31 июля подтвердил все три цели: `market_click`,
  `result_completed` и `mount_detail_click`. Две недостающие цели созданы
  отдельным токеном с минимальными scope; старый токен не изменялся. Готовая
  конфигурация не считается достижением: фактических продуктовых конверсий и
  подтверждённой выручки она сама по себе не создаёт.

## Доказательный fixed-клин ONKRON NN24 — 31 июля

- По правилу portable growth playbook допущен ровно один новый URL
  `/kronshteyny/onkron-nn24/`: строгий запрос `onkron nn24` имеет частоту 218
  по России за последнее доступное 30-дневное окно, официальная карточка
  подтверждает fixed-механизм, 32–65″, 50 кг, 14 схем VESA и отступ 30 мм, а
  точная карточка Маркета прошла проверку наличия и вознаграждаемости.
- Каталог теперь содержит 23 кронштейна и 1 405 verified-fit связей; sitemap —
  141 canonical URL. Остальная массовая генерация остаётся замороженной до
  зрелой query→page выборки.
- Аудит 80 моделей × 3 механизма показал рост сценариев с живым оффером на
  первом месте со 152/240 до 225/240; для fixed — с 4/80 до 77/80. Ни
  `fit_status`, ни технический score, ни число точных механизмов не ухудшились.
  Top-3 coverage осталось 577/719: NN24 улучшает порядок, а не число ссылок.
- Коммит `86977d4f36ab7eea45bc2fb9f5896fd4d86d433d` опубликован успешным GitHub
  Pages run `30614471400`. Новая карточка, sitemap и три публичных affiliate
  snapshot побайтно совпали с production; HTTPS отвечает 200, TLS verify — 0.
- Полная сборка прошла 223 теста. Изолированный production E2E для `TCL 55C7K
  / fixed` показал первым ONKRON NN24 с `verified-fit` и живой карточкой,
  затем P4F без оффера и ATLANTIS-45 с оффером. Хвост закрыт, на ширине 390 px
  нет переполнения, числовых цен и удалённого длинного дисклеймера.
- ONKRON-хаб показывает три проверенных прямых ссылки Маркета. В общем
  коммерческом хабе NN24 fail-closed скрыт для отдельного placement VID,
  поэтому там остаются два живых предложения; недоступная ссылка не
  публикуется.
- После production-проверки IndexNow принял ровно новый URL с HTTP 202. Это
  уведомление, не индексация, показ или посетитель.
- Официальная синхронизация заказов до `2026-07-31T07:57:50.072Z`: 0 заказов,
  0 в карантине, 0 ожидают решения и 0 ₽ подтверждённого вознаграждения.
  Финансовый результат остаётся 0 ₽ из 100 000 ₽ в месяц.

## Новый host Яндекс Вебмастера и переобход — 31 июля

- API подтверждает `verified: true`, состояние проверки `VERIFIED`, но
  `host_data_status: NOT_LOADED`. Это означает, что данные нового host ещё не
  загружены в Вебмастер; это не `NOT_INDEXED`, не ошибка TLS и не отсутствие
  прав.
- Добавленный пользователем `https://krepitv.ru/sitemap.xml` присутствует с ID
  `38ce5dfc-0407-36ec-bf82-b342e24b86fb`, но ещё отсутствует в списке
  загруженных sitemap. Главная, robots, sitemap и verification-файл отвечают
  YandexBot кодом 200.
- Официальный priority sitemap recrawl не запустился: API вернул 404
  `SITEMAP_NOT_FOUND`, потому что файл ещё не появился в обработанном списке.
  Запрос не израсходовал подтверждённую квоту переобхода sitemap.
- Выполнен предусмотренный fallback: ровно главная страница отправлена в
  очередь переобхода. API принял запрос HTTP 202 и выдал task ID
  `5b710280-8cb8-11f1-90a7-cba8414bebe9`; последующая проверка показала
  `IN_PROGRESS`.
- HTTP 202 и `IN_PROGRESS` означают только принятую задачу обхода. Даже `DONE`
  будет доказывать посещение URL роботом, но не индексацию, показы, клики или
  органического посетителя. До завершения этой задачи массовый IndexNow и
  повторная отправка 141 URL не выполняются.

## SEO-волна по VESA и розеткам — 31 июля

- Коммит `5f8c17758f4e1c87c9f64cf61a9946b1ec155fcf` опубликован успешным
  GitHub Pages run `30625470841`. Sitemap остался на **142 canonical URL**:
  новый URL не создавался.
- В существующем `/vesa/` первым режимом стал поиск размера по всем 80 точным
  моделям с официальными источниками и открытым CSV/JSON. Полная таблица
  скрыта по пяти брендам. Расхождение VESA у Hisense 55U7S PRO останавливает
  автоматический подбор и ведёт только в паспорт модели.
- Существующий `/rozetki-pod-televizor-na-stene/` получил масштабную SVG-схему,
  рассчитанный минимальный сдвиг и продолжение до трёх технически совместимых
  кронштейнов через внутренние карточки. Региональных цен и прямого выхода на
  Маркет из технического результата нет.
- Полный release gate прошёл **246 тестов**. Изолированный production E2E на
  ширине 390 и 1440 px проверил оба VESA-сценария, схему розеток и shortlist:
  horizontal overflow и console/page errors отсутствуют, видимые статусы на
  русском языке.
- Production HTML двух страниц, текущие JS/CSS, sitemap и robots побайтно
  совпали с локальными артефактами; HTTPS отвечает 200, TLS verify — 0, HTTP и
  `www` перенаправляют на apex HTTPS.
- После проверки production IndexNow принял **82** содержательно изменённых
  URL: две страницы инструментов и 80 карточек моделей с новой контекстной
  ссылкой. HTTP 202 означает только приём уведомления, а не обход, индексацию,
  показ, посетителя или доход.
- Метрика за 29–31 июля среди разрешивших аналитику показывает 3 визита и 1
  пользователя: 2 direct и 1 internal. Organic после исключения тестов — 0
  визитов/0 пользователей; три цели — 0. Affiliate Orders API: 0 APPROVED
  orders и 0 коп. подтверждённого вознаграждения. Поисковые данные GSC и
  Вебмастера остаются `unknown`, а не нулём, пока нет API-доступа/загрузки host.
