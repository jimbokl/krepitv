[package]
name = "krepitv-sitegen"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
krepitv-engine = { path = "../engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
